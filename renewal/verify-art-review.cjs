const fs=require('node:fs'),path=require('node:path');module.exports=async({chromium,server,root})=>{await new Promise(r=>server.listen(0,'127.0.0.1',r));const b=await chromium.launch({headless:true,channel:'msedge'});try{const p=await b.newPage({viewport:{width:1400,height:1100}});await p.goto(`http://127.0.0.1:${server.address().port}/.test-output/graphite/review.html`);await p.evaluate(()=>Promise.all([...document.images].map(i=>i.decode())));await p.screenshot({path:path.join(root,'.test-output/graphite/review.png'),fullPage:true});}finally{await b.close();server.close();}};
