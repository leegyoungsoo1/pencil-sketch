# 팬심 한 자루 — 크로키 업그레이드

## 선택형 업그레이드 모드 (2026-09-15)

프로그램의 기본 실행은 기존 방식 그대로입니다. 상단 ‘제작 방식’에서 **업그레이드 크로키 (선택)**를 고르면 이 문서의 기능을 사용합니다. 업그레이드 화면에서는 **화가의 크로키 · 한 획씩 그리기**가 기본입니다. 사진 명암을 넓은 마스크로 드러내던 이전 방식은 별도 옵션으로 남겼습니다.

크로키는 사진에서 추출한 윤곽 경로를 연필 선으로 직접 그립니다. 큰 구도 → 얼굴·손·몸 윤곽 → 머리카락과 필요한 그림자 빗금 → 손글씨·서명 순서로 진행합니다. 완성 사진을 덮어씌우거나 끝에 사진 레이어를 나타내지 않습니다. 시작의 원본 소개 장면은 별개이며 설정에서 끌 수 있습니다.

- 필압과 가는 겹선을 사용하며, 머리카락은 서로 겹치지 않는 빗금으로 어두움을 쌓습니다.
- 크로키의 잉크는 소프트 마스크로 반복 지우지 않습니다. 순차 재생과 장면 탐색이 같은 결과를 만듭니다.
- 얼굴과 손·컵이 잘리지 않도록 원본 전체 구도가 기본입니다. 가로 사진은 세로 종이에 여백이 생깁니다. 세부 설정에서 얼굴 확대를 선택할 수 있습니다.
- 그림자 해칭 양을 0으로 하면 크로키의 해칭이 꺼집니다.
- 원본 비교, 장면 탐색, 완성 PNG 저장, 갤러리/나무 작업실 배경을 지원합니다.
- 기본 30초, 4~60초, 1080×1920, 30fps, 목표 18Mbps입니다. 연필 소리가 포함됩니다.

## 실행

이 폴더에서 `python -m http.server 8765 --bind 127.0.0.1`을 실행하고 <http://127.0.0.1:8765>를 엽니다. 기존 공개 웹사이트에는 아직 배포하지 않았습니다.

사진 선택 → 세부 설정에서 크로키 확인 → 미리보기/장면 탐색 → MP4 저장 순서로 사용합니다. 기존 선화 AI는 기기에서 계산하며 선 경로 추출에 사용합니다. 모델을 불러오지 못하면 기존 사진 윤곽 추출을 사용하므로 결과가 달라질 수 있습니다. 새 유료 이미지 API는 추가하지 않았습니다.

## 사용자 사진 결과와 검증

사진 3장(옆얼굴과 컵, 손을 든 장면, 감은 눈 정면)으로 재검증합니다. 결과는 `.test-output/croquis/review.html`, 영상은 같은 폴더의 `1-portrait.mp4`~`3-portrait.mp4`입니다. 이전 명암 드러내기 결과는 `.test-output/portraits`에 남아 있습니다.

- `node test-browser.cjs --portraits --croquis --stills`: 실제 업로드, 선 누적/탐색 재현성, 사진 레이어 미사용 검사와 이미지 저장
- `node test-browser.cjs --portraits --croquis`: 위 검사와 실제 30초 MP4 저장
- `node test-browser.cjs --inspect-videos --croquis`: 저장 파일을 다시 읽어 해상도·길이·29초 장면 확인
- `node test-browser.cjs`: 기존 기능 회귀 검사

이 검사는 특정 화가의 그림 실력이나 관객 반응을 보장하지 않습니다. 자동으로 추출한 윤곽이므로 사람 화가의 선 선택과 완전히 같지는 않습니다. 특히 흐린 손, 어두운 눈, 작은 얼굴의 닮음은 사진마다 확인이 필요합니다.

## 참고와 음악

사용자가 제공한 링크에서 확인한 [이연LEEYEON 드로잉 영상](https://www.youtube.com/watch?v=JQt_l5wgjco)의 선과 빗금이 쌓이는 과정을 참고했습니다. 영상이나 그림 자체를 앱 자산으로 복사하지 않았습니다.

노래는 YouTube 쇼츠 제작 도구에 제공되는 음원을 선택하는 방식으로 추가하세요. 외부 노래 파일을 직접 합치면 일반 Content ID 청구 대상이 될 수 있습니다. [YouTube 안내](https://support.google.com/youtube/answer/13053317?hl=ko). 수익 창출은 [YouTube 수익화 정책](https://support.google.com/youtube/answer/12504220?hl=ko)과 콘텐츠 권한을 충족해야 합니다.


## 손목과 팔 움직임 수정 (2026-09-15)

- 손가락 이완 동작, 쉬는 시간, 그립 사진 전환을 제거했습니다.
- 사진 09의 손·연필·손목·팔을 한 이미지로 연결합니다. 카메라 밖으로 잘린 부분은 원본 팔 가장자리의 색과 질감을 바탕으로 연장했습니다.
- 연필 끝을 기준으로 작은 손목 회전과 팔의 연속적인 변형을 적용합니다. 확대·축소 효과는 없으며 획 사이 들림은 최대 2픽셀입니다.
- ‘내 손과 팔로 그리기’, ‘부드러운 손목 움직임’으로 제어합니다.
- 이번 검증은 요청대로 첫 번째 인물 사진만 사용했습니다. 결과는 `.test-output/arm/review.html`, 영상은 `1-portrait.mp4`입니다.
- 영상 생성: `node test-browser.cjs --portraits --croquis --arm --first`
- 저장 파일 검증: `node test-browser.cjs --inspect-videos --arm --first`
- 손목 제어·팔의 화면 밖 연결·연필 끝 접촉 검사: `node test-browser.cjs --hand-controls`
- 추출 과정: `tools/extract-arm.py`, 자산 설명: `assets/hands-v3/README.md`.


## 이전 실험: 팔 두께와 높이별 손목 보정 (현재 미사용)

- 현재 자산은 `assets/hands-v4/draw-arm.png`입니다. 손·연필은 보존하고 손목 아래 팔 폭을 최대 1.5배까지 연속적으로 넓힙니다.
- 화면 아래쪽에서는 기존 굽힘을 유지하고 위쪽일수록 팔이 손과 정렬되도록 메시 변형을 추가했습니다. 별도의 이미지 생성이나 사진 전환은 없습니다.
- 첫 번째 사진 테스트: `node test-browser.cjs --portraits --croquis --arm-v4 --first`
- 검증: `node test-browser.cjs --hand-controls`, `node test-browser.cjs --inspect-videos --arm-v4 --first`
- 결과: `.test-output/arm-v4/review.html`. 이전 결과는 보존합니다.


## 현재 적용: 처음 팔 이미지로 복원

사용자 요청으로 `assets/hand-right.webp`로 되돌렸습니다. `hand-motion.js`는 연필 끝을 기준으로 이미지 전체의 회전만 적용하며, 확대율은 고정입니다. 메시 변형·폭 보정·피부 연장 자산·추가 그림자·휴식 동작은 현재 앱에서 사용하지 않습니다. 원래 자산 자체는 수정하지 않았습니다.

결과는 `.test-output/original-arm/review.html`이며 첫 번째 사진만 30초 MP4로 생성합니다. 명령: `node test-browser.cjs --portraits --croquis --original-arm --first`, 저장 검증: `node test-browser.cjs --inspect-videos --original-arm --first`. 이전 v3/v4 결과는 비교용으로 보존합니다.


## 획 속도와 소리 동기화

- 팔 전체 회전 범위를 늘렸습니다. 원래 팔 이미지는 고정 비율로 표시합니다.
- 긴 윤곽선의 시간 배분을 늘리고 해칭은 빠르게 처리합니다. 방향 변화가 큰 구간은 감속하며, 거리 기반 진행표와 시작/끝 가감속을 사용합니다. 전체 영상 길이는 유지됩니다.
- 각 획의 실제 이동 속도를 샘플링해 연필 소리의 음량과 필터 주파수를 만듭니다. 미리보기와 MP4는 동일한 진행표·소리 이벤트를 사용하고, 획 사이에는 음량이 0입니다.
- 이번 결과: `.test-output/rhythm/review.html`. 첫 번째 사진만 사용했습니다.
- 재현: `node test-browser.cjs --portraits --croquis --rhythm --first`
- 저장 검증: `node test-browser.cjs --inspect-videos --rhythm --first`


## 기존 프로그램 보존 및 옵션 분리

- `index.html`은 기존 화면에 제작 방식 선택만 추가했습니다. 기본값은 기존 방식입니다.
- `app.js`와 `style.css`는 작업 전 Git 커밋의 내용 그대로 보존했습니다.
- 선택 화면: `upgraded.html`, 실행 코드: `app-upgraded.js`, 스타일: `style-upgraded.css`. 두 방식은 실행 코드를 서로 로드하지 않습니다.
- 옵션 변경은 페이지를 이동하므로 먼저 방식을 선택한 뒤 사진을 불러오세요. 기본 모드로 돌아가는 선택도 제공합니다.
- `node test-browser.cjs --modes`: 기본값·양방향 전환·모바일 레이아웃 검증.

## 편지·배경·롱폼 확장 (2026-09-15)

기존 프로그램은 보존하고 아래 기능은 `upgraded.html`에만 적용했습니다.

- 서명: `David Lee.` 기본값, `Yoonseul Lee.` 선택 및 직접 수정.
- 손글씨 편지: 12줄 입력창, 최대 4,000자와 자동 줄바꿈. 여러 줄은 별도 편지지에 쓰며, 줄 수와 권장 시간을 표시합니다. 16줄을 넘으면 글씨가 작아집니다.
- 제목 글꼴 5종과 빈 팬심 문구 선택. 화면 맨 아래의 중복 문구는 제거했습니다.
- 이미지 생성 도구로 제작한 배경 10종을 미리보기로 선택합니다. 자산 목록은 `assets/backgrounds/manifest.json`입니다. 도구에 모델 지정 기능이 없어 정확한 모델 버전은 확인할 수 없습니다.
- 세로 1080×1920, 가로 1920×1080. 사진 여러 장을 추가하고 순서를 변경하거나 삭제할 수 있습니다.
- 영상 길이는 사진 한 장당 4~600초입니다. 여러 사진의 총 길이를 표시하고 한 영상으로 연결합니다. 편지·제목·서명은 모든 사진에 공통 적용됩니다.
- 완성 사진 PNG 저장은 현재 선택한 사진을 저장합니다. 영상 저장은 목록 전체를 연결합니다.
- 오디오는 30초 단위로 렌더링해 긴 영상의 비압축 오디오 메모리를 줄였습니다. 최종 압축 영상은 브라우저 메모리에 구성하므로 전체 길이에는 컴퓨터 메모리의 영향을 받습니다.
- `node test-browser.cjs --project`: 서명, 빈 문구, 12줄 편지, 배경 10종, 사진 순서, 가로·세로, PNG, 2장 연결 MP4, 60초 오디오 및 첫 번째 사진 영상 저장 검증.


## 편지 위치·가로 팔·소리 수정 (2026-09-15)

- 여러 줄 편지를 별도 종이 대신 그림 위에 직접 씁니다. 가로·세로 모두 한 장의 종이를 사용합니다.
- 가로 영상은 원본 팔 사진의 오른쪽 잘린 경계가 화면 아래로 나가도록 회전합니다. 원본 비율과 연필 끝 위치를 유지하며 피부 합성이나 늘리기는 하지 않습니다.
- 연필 소리의 고음 강조와 거친 잡음, 기본 음량을 낮췄습니다. 획 속도에 따른 변화와 펜을 뗄 때의 무음은 유지합니다.
